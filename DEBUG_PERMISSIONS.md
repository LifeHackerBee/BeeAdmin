# 权限调试指南

## 🔍 调试步骤

### 1. 重新构建前端（必需）

```bash
cd BeeAdmin
pnpm run build

# 或使用 Docker
cd ..
docker-compose up -d --build bee-admin
```

### 2. 清除浏览器缓存
- 按 F12 打开开发者工具
- 右键刷新按钮 → "清空缓存并硬性重新加载"
- 或使用无痕模式

### 3. 登录并查看控制台日志

使用 `xmandongdong@hotmail.com` 登录后，打开浏览器控制台（F12），你应该看到类似这样的日志：

```
=== 侧边栏权限调试 ===
用户邮箱: xmandongdong@hotmail.com
用户角色: ["user"]
允许的模块: ["beetrader", "beetrader.tracker", ...]
模块数量: 12
检查菜单项: Dashboard, URL: /, 模块: null
检查菜单项: Apps, URL: /apps, 模块: apps
  -> 模块权限检查失败: apps
检查菜单项: BeeAI, URL: /beeai, 模块: beeai
  -> 模块权限检查失败: beeai
...
过滤后的导航组数量: X
  - General: X 项
  - Trading: X 项
=====================
```

## 📊 预期结果

### 用户信息
- ✅ 用户邮箱: xmandongdong@hotmail.com
- ✅ 用户角色: ["user"]
- ✅ 允许的模块: 包含 beetrader 相关模块（12个）
- ✅ 模块数量: 12

### 菜单项检查
**应该通过的**:
- ✅ Dashboard (URL: /) - 无模块限制
- ✅ BeeTrader 及其所有子菜单
- ✅ Settings - 无模块限制
- ✅ Help Center - 无模块限制

**应该被过滤的**:
- ❌ Apps (URL: /apps, 模块: apps)
- ❌ BeeAI (URL: /beeai, 模块: beeai)
- ❌ Users (URL: /users, 模块: users)
- ❌ FIRE 计算器 (URL: /fire, 模块: fire)
- ❌ 财务管理及子菜单 (URL: /finance/*, 模块: finance.*)
- ❌ 后台任务 (URL: /monitoring/tasks, 模块: monitoring.tasks)

### 过滤后的导航组
应该只看到包含允许访问内容的导航组，空组会被过滤掉。

## 🐛 故障排除

### 问题 1: 控制台没有日志输出

**原因**: 前端代码未重新构建

**解决方案**:
```bash
docker-compose up -d --build bee-admin
```

### 问题 2: allowedModules 为空数组 []

**原因**: 用户需要重新登录以刷新权限

**解决方案**:
1. 退出登录
2. 清除浏览器缓存
3. 重新登录

### 问题 3: allowedModules 显示但权限检查仍然失败

**原因**: 可能是模块名匹配逻辑问题

**调试方法**: 
在控制台运行以下代码查看详细信息：

```javascript
const { auth } = useAuthStore.getState()
console.log('用户信息:', auth.user)

// 测试权限检查
import { hasModuleAccess } from '@/lib/rbac'
console.log('Can access apps:', hasModuleAccess(
  auth.user.role, 
  auth.user.allowedModules, 
  'apps'
))
console.log('Can access beetrader:', hasModuleAccess(
  auth.user.role, 
  auth.user.allowedModules, 
  'beetrader'
))
```

### 问题 4: 菜单项仍然显示但应该被隐藏

查看控制台日志中的 "检查菜单项" 输出，确认：
1. URL 是否正确识别
2. 模块名是否正确提取
3. 权限检查结果是什么

## 📝 关键检查点

1. **allowedModules 数量应该是 12**
   - 如果是 0，说明权限数据未正确加载
   - 需要重新登录

2. **每个菜单项都应该有权限检查日志**
   - 如果没有日志，说明过滤函数未执行
   - 需要重新构建

3. **模块名提取应该正确**
   - `/apps` → `apps`
   - `/beeai` → `beeai`
   - `/beetrader/tracker` → `beetrader.tracker`
   - `/finance/expenses` → `finance.expenses`

4. **权限检查结果应该符合预期**
   - `apps` → 失败
   - `beeai` → 失败
   - `beetrader` → 通过
   - `beetrader.tracker` → 通过

---

## ⏭️ 下一步

1. 重新构建前端
2. 清除缓存并重新登录
3. 查看控制台日志
4. 将日志截图或复制给我，我会帮你分析问题
